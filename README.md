# slow-print-cli
A terminal-based Python program that uses a slow typing function (slow_print) to create an interactive experience. The user answers questions and receives motivational and reflective messages, all displayed with an animated typing effect.
